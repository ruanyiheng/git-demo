# SaaS 短链接项目 面试题整理

> 项目：D:\javapro\short （SaaS 短链接系统 / hippo4j 衍生）
> 技术栈：JDK17 + Spring Boot 3 + Spring Cloud + ShardingSphere + Redis(Redisson) + Redis Stream + MyBatis-Plus

---

## 📸 图片 1

### 1. 自我介绍
> 面试官您好，我叫XXX，毕业于XXX大学XXX专业，有X年Java后端开发经验。技术栈主要围绕Spring Cloud微服务生态（Spring Boot 3 / Nacos / Gateway / OpenFeign），熟悉 MySQL、Redis、RocketMQ、ShardingSphere 等中间件。最近在做的是一个 **SaaS 短链接系统**（hippo4j/shortlink），负责短链生成、跳转、统计、分组分片等核心模块，做到了支持高并发、海量存储、多租户隔离的工业级设计。今天希望能跟您深入聊下短链项目里缓存、分库分表、布隆过滤器这几个我重点投入过的部分。

---

### 2. 个人项目还是公司项目
> 是个人**学习项目**（跟着"拿个offer"作者的视频从零到一写的），但**完全按SaaS工业级标准**做的，覆盖了多租户、海量并发、海量存储、数据隔离、可扩展等真实业务场景，能直接作为校招/社招的亮点项目。

---

### 3. 项目中遇到的难点问题（背景+方案）
**难点①：短链跳转的缓存与数据库一致性**
- **背景**：短链跳转是典型"读多写少"（读QPS >> 写QPS），为了扛高并发，我引入了 Redis 做缓存。但短链支持修改原始URL、有效期、分组，修改时如果不处理缓存，会出现"DB已改、缓存还是旧值"，导致用户跳转错地址。
- **解决方案**：采用 **Cache Aside（旁路缓存）模式**，**先更新数据库，再删除缓存**，并配合"延迟双删"做兜底。
- **项目证据**：`project/.../ShortLinkServiceImpl.java:338`
  ```java
  baseMapper.update(shortLinkDO, updateWrapper);   // ① 先改DB
  // ...
  stringRedisTemplate.delete(                       // ② 再删缓存
      String.format(GOTO_SHORT_LINK_KEY, requestParam.getFullShortUrl()));
  ```

**难点②：海量数据存储**
- **背景**：短链数据可能达百亿级，单表撑不住。
- **解决方案**：用 **ShardingSphere** 按 `gid` 做分片（`HASH_MOD` 算法，16张表），让同分组数据落同一表，"按gid查询"直接命中单分片避免全表扫。配套 `t_link_goto` 路由表（按 `full_short_url` 分片）实现"先路由再查表"的两步跳转。
- **项目证据**：`project/src/main/resources/shardingsphere-config-dev.yaml`
  ```yaml
  t_link:
    actualDataNodes: ds_0.t_link_${0..15}
    tableStrategy:
      standard:
        shardingColumn: gid
        shardingAlgorithmName: link_table_hash_mod
  ```

---

### 4. Redis 缓存一致性方案：先更新DB再删缓存 vs 先删缓存再更新DB
- **项目用的是"先更DB再删缓存"**（Cache Aside 经典做法）
- **为什么不反过来（先删缓存再更DB）**：
  删完缓存后，另一个读线程并发进来，会读到 DB 旧值并**回填到缓存**，随后写线程才更新 DB —— 这时缓存里长期是**旧值**，而且不会自动失效。
- **优缺点对比**：

| 方案 | 优点 | 缺点 |
|---|---|---|
| **先更DB再删缓存**（项目采用） | 写失败时缓存不被错误清空；并发下出现不一致的窗口极小 | 删缓存失败会导致脏缓存（需要MQ重试/Canal补偿） |
| 先删缓存再更DB | 简单直接 | 并发读会把DB旧值回填到缓存，长期不一致 |
| 延迟双删 | 能解决"回填旧值"问题 | 性能损耗（sleep），强一致仍无法保证 |

- **项目里更稳的兜底**：删缓存失败可走MQ异步重试 / Canal 订阅 binlog 二次删除。

---

### 5. Redis 删除数据指令
- `DEL key`：同步删除，立即释放内存，**大 key 会阻塞主线程**
- `UNLINK key`（Redis 4.0+）：**异步删除**，O(1) 立即返回，后台线程慢慢释放内存，大 key 友好
- 项目里调用的是 `stringRedisTemplate.delete(key)`，底层对应 `DEL`，因为 value 是短链字符串不大，同步删除没问题；如果是排行榜、长列表等大 key 建议 `UNLINK`。

---

### 6. MySQL 单核 QPS
- **简单主键等值查询**（InnoDB buffer pool命中）：单实例 MySQL 8 大约 **4000~5000 QPS**（4核8G、千兆网络）
- **复杂查询 / 多表JOIN / 范围扫**：跌到 **500~1000 QPS**
- **写TPS**（含事务、redo log、binlog）：单实例约 **1500~2500 TPS**
- **短链项目里**扛高QPS靠 Redis 缓存（命中率99%+），MySQL 分片只做最终兜底，单分片压力不大。

---

### 7. 分组 gid 的类型
- **varchar(32) 字符串**，**不是** int 自增
- **项目证据**：`resources/database/link.sql` 里 `gid varchar(32) DEFAULT NULL`；`admin/.../GroupDO.java:46` `private String gid;`
- 原因：① 分组ID要支持自定义和分布式生成（雪花ID风格），② 字符串可携带业务前缀（如 `g_xxx`），③ 32位长度能容纳 64位 long 的字符串形式

---

### 8. 选 gid 作分片键可能没达到分表目的
- **确实存在**。按 `gid` 分片，**热点 gid 的数据会集中到一个分片**，造成：
  - **数据倾斜**：少数大V用户/热门活动的 gid 数据量巨大
  - **读热点**：同一个 gid 被反复访问，缓存击穿后单分片 MySQL 压力大
- **热点本质跟"分组数量"无关，跟"访问频率"有关** —— 即使用户分组数有限，但单个爆款短链的访问QPS可能占全站50%
- **更合理的方案**（实际生产中怎么做）：
  1. **读路径多级缓存**：Caffeine 本地缓存 → Redis 集群 → MySQL分片（本地缓存扛热点，Redis 扛均匀流量，DB 只兜底）
  2. **热点数据单独存储**：检测到 QPS 超过阈值的 gid，单独放"热点表" + 单独 Redis 预热
  3. **VIP 通道**：给热点短链/付费用户分配独立分片 + 独立缓存集群
  4. **项目里已有的防护**：`RBloomFilter` 拦穿透 + 空值缓存（`GOTO_IS_NULL_SHORT_LINK_KEY`）拦击穿，挡住大部分无效DB请求

---

### 9. `select * from table where a=1 and b=2` 怎么建索引
- **建联合索引 `idx_a_b(a, b)`**，**不**建议 a、b 各建一个单列索引（MySQL 一次只会选一个，另一个变回表）
- **索引是什么**：一种**有序数据结构**（InnoDB 用 B+ 树），类似书的目录，把 O(n) 全表扫变成 O(log n) 查找
- **为什么能加速**：
  - B+ 树是**多路平衡树**，树高低（千万级数据也就3~4层）
  - **叶子节点是链表**，范围查询顺序IO
  - 数据按主键聚簇，二级索引叶子存的是主键（回表）
- **最左前缀原则**：`idx(a,b)` 等价于建了 `(a)` 和 `(a,b)`，但**不**等价于 `(b)`
- **索引失效常见坑**：① 对索引列用函数/表达式 `where YEAR(create_time)=2024`；② 隐式类型转换；③ 前导模糊 `like '%xx'`；④ 联合索引不满足最左前缀；⑤ `OR` 连接的列无索引
- `select *` **无法**走覆盖索引（覆盖索引要求查询列都在索引里），但本题主要考察 **a、b 的索引设计**

---

### 10. 手撕：两个有序数组合并
**思路**：双指针 + 辅助数组，谁小拷贝谁，最后哪个数组有剩余就直接追加。
**边界**：必须处理「数组为 null」「数组为空」「两边都为 null」三种情况，否则会 NPE。

```java
public static int[] merge(int[] a, int[] b) {
    // 1. 防御性校验：null / 空 直接返回另一边
    if (a == null || a.length == 0) {
        return b == null ? new int[0] : b;
    }
    if (b == null || b.length == 0) {
        return a;
    }
    // 2. 双指针归并
    int[] res = new int[a.length + b.length];
    int i = 0, j = 0, k = 0;
    while (i < a.length && j < b.length) {
        if (a[i] <= b[j]) {
            res[k++] = a[i++];
        } else {
            res[k++] = b[j++];
        }
    }
    // 3. 处理尾巴
    while (i < a.length) res[k++] = a[i++];
    while (j < b.length) res[k++] = b[j++];
    return res;
}
```

**面试官追问**：
- "别人传了空指针，你这代码会报错吗？" → **不会**。方法开头三个 if 已经把 null/空 全部拦截，最坏返回 `new int[0]`，调用方不会拿到 NPE。
- 时间复杂度 O(m+n)，空间复杂度 O(m+n)，是稳定排序（用 `<=` 保证相等元素相对顺序不变）。

---

## 📸 图片 2

### 1. 自我介绍
> 同图片1答案（自我介绍通常准备一个标准版本，1分钟左右，讲清 姓名/经验/技术栈/最近项目/亮点模块）。

---

### 2. 项目是学习项目还是？
> 是个人**学习项目**（跟随"拿个offer"作者的SaaS短链接视频从零到一搭建的），但**完全按工业级SaaS标准**设计：多租户隔离、分库分表、缓存与数据库一致性、布隆过滤器防穿透、消息队列削峰、多级缓存等都用到了，可作为校招/社招的亮点项目。

---

### 3. 原始长链接存储在哪？（数据库+Redis）
> **双写：DB 主存储 + Redis 缓存**
> - **数据库**：原始长链接（`origin_url`）存在 `t_link` 表里（已按 `gid` 分16张表），是**权威数据源**。
> - **Redis 缓存**：跳转前会先查 Redis（`short-link:goto:{fullShortUrl}` → 原始URL），未命中才查 DB，然后回填 Redis。
> - **项目证据**：`project/.../ShortLinkServiceImpl.java:160-164`（创建时同步写缓存）、`:383`（跳转时先读缓存）。
> ```java
> // 写缓存
> stringRedisTemplate.opsForValue().set(
>     String.format(GOTO_SHORT_LINK_KEY, fullShortUrl),
>     requestParam.getOriginUrl(),
>     LinkUtil.getLinkCacheValidTime(requestParam.getValidDate()), TimeUnit.MILLISECONDS
> );
> ```

---

### 4. 布隆过滤器是自己实现的，还是用的开源的实现？
> **用的开源实现：Redisson 的 `RBloomFilter`**
> - **项目证据**：`project/.../RBloomFilterConfiguration.java`
> ```java
> @Bean
> public RBloomFilter<String> shortUriCreateCachePenetrationBloomFilter(RedissonClient redissonClient) {
>     RBloomFilter<String> cachePenetrationBloomFilter =
>         redissonClient.getBloomFilter("shortUriCreateCachePenetrationBloomFilter");
>     cachePenetrationBloomFilter.tryInit(100000000L, 0.001); // 1亿容量，0.1%误判率
>     return cachePenetrationBloomFilter;
> }
> ```
> - **选 Redisson 原因**：① 分布式BitMap实现，可跨实例共享；② API简洁（`tryInit / add / contains`）；③ 与项目使用的Redis客户端一致，省去二次集成。

---

### 5. 这两个项目，有哪一块是你投入了比较多的精力和思考？
> 短链项目里的**跳转功能**（`restoreUrl`）是我投入最多的部分，它把整个项目的核心技术串起来：
> - **缓存穿透防护**：先用布隆过滤器判断短链是否存在
> - **缓存击穿防护**：用分布式锁 + 空值缓存双层兜底
> - **缓存与数据库一致性**：跳转路径全部"先查缓存，未命中才查DB"
> - **统计异步化**：跳转不阻塞统计，统计消息扔 Redis Stream 异步落库
> - **代码位置**：`project/.../ShortLinkServiceImpl.java:371-442`

---

### 6. 一致性问题怎么解决的？为什么不反过来（先删缓存再更新DB）？
> 答：采用 **Cache Aside + 先更DB再删缓存** 的策略。
> **为什么不用"先删缓存再更DB"**：
> - 时序：①删缓存 → ②并发读线程读DB旧值 → ③回填旧值到缓存 → ④写线程更新DB
> - 后果：**缓存里长期是旧值**，且不会自动失效
> - 反过来采用"先更DB再删缓存"的时序：①更新DB → ②删缓存 → ③下次读未命中 → ④从DB读新值回填
> - 即使删缓存失败，也只是会读到旧值一次（直到TTL过期），不会长期污染缓存

---

### 7. 布隆过滤器的位数组构建过程？什么步骤填充？怎么维护？
> **位数组构建（初始化）**：
> 1. 预估容量 `n`（项目里是 1 亿条短链）
> 2. 设定期望误判率 `p`（项目里是 0.001 = 0.1%）
> 3. 通过公式算出位数组大小 `m = -n*ln(p)/(ln2)^2`、哈希函数个数 `k = (m/n)*ln2`
> 4. Redisson 的 `tryInit(n, p)` 内部就是分配一段连续bit位 + 注册k个哈希函数
>
> **填充时机**（项目里两步）：
> - **创建短链接成功后**填充：`ShortLinkServiceImpl.createShortLink` 第166行 `shortUriCreateCachePenetrationBloomFilter.add(fullShortUrl);`
> - **跳转前判断存在性**：`restoreUrl` 第389行 `boolean contains = shortUriCreateCachePenetrationBloomFilter.contains(fullShortUrl);`
>
> **维护方式**：
> - **添加**：仅在"创建短链接"路径追加
> - **删除**：布隆过滤器**不支持删除**（这是它的固有限制，见题8的解决方案）
> - **重建**：当位图占用/误判率超阈值时全量重建（见题9）

---

### 8. 短链对应的长链接会发生变更吗？MySQL数据变了，布隆过滤器怎么跟着变？
> **会发生变更**。短链支持修改原始URL、有效期、修改分组。
>
> **关键点**：布隆过滤器**存的是"短链是否存在"**，**不存**短链和长链的映射关系。
> - MySQL里 `origin_url` 变了 → 布隆过滤器**不需要改**，因为它只判断"这个短链key是否合法"
> - 跳转时拿短链key去查缓存/DB → 拿到的是 **最新的长链接**
> - **只有"短链被删除"才会影响布隆过滤器**：被删除的短链在过滤器里仍然会返回"存在"，会**多查一次DB**（查到空，再回填空值缓存）。**这不会导致错误，只会导致少量性能损失**。
>
> **结论**：长链接变更 → 布隆过滤器**无需感知**；短链删除 → 容忍少量"假阳性"穿透，不影响正确性。

---

### 9. 布隆过滤器不支持删除，长期运营后误判率上升怎么办？
> - **首先澄清**：布隆过滤器的误判率是**初始化时按 `tryInit(n, p)` 锁定的**，不会因为"长期运营"自动上升。误判率上升的**唯一场景**是**实际数据量超过预设容量 n**（项目里 n=1亿），导致bit位被反复置1，几乎全部key都判"存在"。
> - **应对方案**：
>   1. **容量预留**：初始化时给 n 预留 2~3 倍冗余（项目里就是 1 亿，业务量 < 5千万 都很安全）
>   2. **监控位图使用率**：通过 Redis `BITCOUNT` 统计已置1的位数 / 总位数，超过 50% 触发告警
>   3. **扩容重建**：申请一个 2 倍容量的新布隆过滤器，异步全量回填（用 `BF.RESERVE` + 批量 `BF.ADD`），切换读路径到新过滤器；老过滤器保留观察期后再释放
>   4. **双写过渡期**：重建期间新过滤器接受 add，老过滤器继续服务读，新老并存

---

### 10. 设置多个布隆过滤器能解决"原数据改了访问不到"的问题吗？
> **严格来说解决不了**，但可以**降低影响**：
> - **问题本质**：短链对应的长链接变更，**布隆过滤器根本不需要感知**（它只判断"短链是否存在"），所以"原数据改了"和布隆过滤器无关。
> - **真正的问题**是"短链被删/失效"：原短链在布隆过滤器里仍返回"存在"，导致多查一次DB。
> - **多布隆过滤器方案（如 a/b 两个）**：
>   - **扩容场景**：数据量翻倍时，a 满就上 b，业务无感（**这部分能解决**）
>   - **"原始数据改了"场景**：你描述的"新数据添加到b里，读路径访问a访问不到"——**这个不是布隆过滤器的设计问题**，因为布隆过滤器**不存数据内容**，只存存在性。**所以严格说还是要走"重建 + 切换"的过程**，多布隆过滤器主要是为扩容和灰度切换服务。

---

### 11. 怎么评价布隆过滤器的误判情况？项目里有没有做？
> **项目里目前没做线上误判率统计**（这点要诚实说），但**理论上的评估方法**有：
> 1. **数学公式估算**：`p = (1 - e^(-kn/m))^k`，m=位数组大小, n=实际元素数, k=哈希函数个数。已知 m/k 就能算出当前误判率。
> 2. **埋点观测法**：在 `contains` 返回 true 后**真的去查DB**，统计"布隆说存在但DB查不到"的次数 / 总"布隆说存在"的次数 → 真实误判率
> 3. **位图使用率监控**：`BITCOUNT` 算出已置1的位数，`使用率 = 已置1位数 / 总位数`，使用率越高，误判率越大
> 4. **权衡**：误判率越低 → 位数组越大 → Redis内存越大。一般生产中 **0.1% ~ 1%** 是性价比甜蜜点
>
> **答面试官建议**：诚实说"项目里没做线上误判率统计，但了解评估方法，生产中可以通过埋点 + BITCOUNT 监控实现"。

---

### 12. 怎么判断布隆过滤器"什么时候不好用了"需要刷新？
> **判断依据**有三种（按可靠性从高到低）：
> 1. **空缓存命中次数激增**（最直接）：维护一个"布隆说存在但查DB为null"的计数器
>    - 正常情况下这个值很小（只有新增短链后第一次穿透时为空）
>    - 如果这个值**持续增长**，说明布隆过滤器在大量"误判存在"
> 2. **位图使用率告警**：`BITCOUNT` 超过 50% 时告警，超过 70% 必须重建
> 3. **业务指标**：`restoreUrl` 接口的 P99 延迟、QPS 异常突增（因为穿透到DB的请求变多）
>
> **本项目里**：通过**空值缓存命中次数**间接判断（每次DB查空都会写 `GOTO_IS_NULL_SHORT_LINK_KEY`），统计这个key的访问频率就能反推误判率趋势。

---

### 13. 引申：空缓存有时效，MySQL数据不变时怎么评判布隆过滤器刷新后一定变好？
> **这其实是"评测策略"问题**，光刷新不够，**还要有"刷新前后对比"机制**：
> - **不能只看瞬时误判率**（刷新完那一刻的快照意义不大）
> - **要维护"误判率变化趋势"** —— 这就是所谓的**状态感知**
> - **具体做法**：
>   1. **滑动窗口统计**：在 `restoreUrl` 里加埋点，记录 "布隆判存在→DB查空" 的事件
>   2. **滚动计算误判率**：1分钟滑动窗口的 误判数 / 总布隆判存在数
>   3. **刷新前后对比**：刷新前采集一周基线，刷新后同窗口对比，**误判率下降**才算优化有效
>   4. **空缓存的时效**：空缓存确实会过期（项目里设了30分钟），但**误判率统计埋点是"事件级"**的，**不依赖空缓存是否还在**。空缓存只是优化手段，**误判率统计才是评判标准**。
> - **一句话总结**：评判布隆过滤器好坏的不是"它判了什么"，而是"它判错的频率"，这需要持续的指标采集和趋势对比。

---

### 手撕1：子数组中最大值在两端
> **题目**：给一个数组 `[1,3,3,4,2]`，求所有满足"子数组中的最大值出现在子数组两端"的子数组。
> 例如 `[1,3,3,4,2]` 中：`[1]`、`[4]`、`[3]`、`[2]`、`[3,3]` 等。
>
> **思路**（单调栈）：
> - 找出每个元素作为"最大值"时能覆盖的最左/最右边界 → 用**单调递减栈**求左右第一个大于它的位置
> - 遍历每个元素，以它为最大值的子数组 = `left~right` 范围内任意子数组
> - 当这个最大值是**子数组两端的某个**时，要求子数组必须以这个元素为起点或终点
>
> **思路简化（推荐面试答法）**：先求每个元素的"贡献区间" `[L, R]`（以它为最大值的最长区间），那么满足条件的子数组就是**包含该元素，且以该元素为左端或右端**的子数组：
> ```java
> // 伪代码
> for (int i = 0; i < n; i++) {
>     int L = leftGreater[i] + 1;  // 左边第一个大于nums[i]的下一个位置
>     int R = rightGreater[i] - 1;  // 右边第一个大于nums[i]的前一个位置
>     // 包含i且最大值在两端 → 起点为i或终点为i
>     // 起点=i: 子数组 [i, j], j ∈ [i, R]   共 (R-i+1) 个
>     // 终点=i: 子数组 [j, i], j ∈ [L, i]   共 (i-L+1) 个
>     // 重复算 [i,i]: 减1
>     res += (R - i + 1) + (i - L + 1) - 1;
> }
> ```

---

### 手撕2：找出 1~n 中所有"自恋数"
> **题目**（变形：水仙花/自恋数）：给定 n（如 37），从 1~n 中找出满足"数位拆分之和等于其平方数位拆分之和"的数。
> 例：`1*1=1 → 1+0+1=2`? 不对，例子再读一下：`1*1=1 → 1`；`9*9=81 → 8+1=9`；`36*36=1296 → 1+2+9+6=18`? 等等，**例子要求输出 1+9+36=46**。
> **理解**：可能规则是"i*i 的各位数字之和 = i"，找出所有这样的 i，输出它们的和。
> 验证：`1*1=1` → 1=1 ✓；`9*9=81` → 8+1=9 ✓；`36*36=1296` → 1+2+9+6=18 ✗（不等于36）
> 哦不对，再读一遍——例子是"36*36=1296，可以拆为 1+2+9+6=18=？"，**最后输出 1+9+36=46**。所以"1, 9, 36" 都是满足"i*i 的各位数字之和 = i"的数。
> **代码**：
> ```java
> public static int findNumbers(int n) {
>     int sum = 0;
>     for (int i = 1; i <= n; i++) {
>         int sq = i * i;
>         int digitSum = 0;
>         while (sq > 0) {
>             digitSum += sq % 10;
>             sq /= 10;
>         }
>         if (digitSum == i) {
>             sum += i;
>         }
>     }
>     return sum;
> }
> ```
> **思路**：枚举 1~n，对每个 i 算 `i*i` 的各位数字之和，与 i 比较，相等就累加。

---

## 📸 图片 3

### 手撕：删除链表中所有重复元素，只保留出现一次的
> **题目**：链表 `1→2→3→3→4→4→5`，删除所有重复过的节点，只保留出现一次的：`1→2→5`。
>
> **思路**：单指针无法判断"重复次数"，需要**两次遍历/哈希**。最优雅的是**一次遍历 + 哈希表记录频次**，再**重建新链表**。
>
> **核心代码**：
> ```java
> public ListNode deleteDuplicates(ListNode head) {
>     Map<Integer, Integer> cnt = new HashMap<>();
>     // 第一次遍历：统计频次
>     for (ListNode p = head; p != null; p = p.next) {
>         cnt.merge(p.val, 1, Integer::sum);
>     }
>     // 第二次遍历：构建新链表
>     ListNode dummy = new ListNode(0);
>     ListNode tail = dummy;
>     for (ListNode p = head; p != null; p = p.next) {
>         if (cnt.get(p.val) == 1) {
>             tail.next = new ListNode(p.val);
>             tail = tail.next;
>         }
>     }
>     return dummy.next;
> }
> ```
>
> **进阶：O(1) 空间解法**（面试官追问用）：
> ```java
> public ListNode deleteDuplicates(ListNode head) {
>     ListNode dummy = new ListNode(0, head);
>     ListNode pre = dummy, cur = head;
>     while (cur != null) {
>         // 如果cur.next存在且等于cur，cur就是重复的
>         if (cur.next != null && cur.val == cur.next.val) {
>             int x = cur.val;
>             // 跳过所有值等于x的节点
>             while (cur != null && cur.val == x) cur = cur.next;
>             pre.next = cur;
>         } else {
>             pre = cur;
>             cur = cur.next;
>         }
>     }
>     return dummy.next;
> }
> ```

---

### 八股 1. Java 集合有哪些？ArrayList/HashMap/HashTable/HashSet 线程安不安全？底层结构？
> **List 系列**：
> - `ArrayList`：基于**动态数组**实现，**线程不安全**（`add` 非原子，并发写可能丢数据或数组越界），**查询O(1)，增删O(n)**
> - `LinkedList`：基于**双向链表**，**线程不安全**，**查询O(n)，增删O(1)**
> - `Vector`：基于**动态数组** + `synchronized` 方法，**线程安全但性能差**，基本被淘汰
> - `CopyOnWriteArrayList`：写时复制，**线程安全**，适合**读多写少**场景
>
> **Set 系列**：
> - `HashSet`：底层是 **`HashMap`**，value 统一是 PRESENT 占位对象，**线程不安全**
> - `LinkedHashSet`：继承 `HashSet`，内部 `LinkedHashMap`，**保持插入顺序**
> - `TreeSet`：底层红黑树，可排序
> - `CopyOnWriteArraySet`：线程安全
>
> **Map 系列**：
> - `HashMap`：JDK8 之前是**数组+链表**，JDK8 之后是**数组+链表+红黑树**（链表长度 > 8 且数组长度 ≥ 64 时树化），**线程不安全**
>   - 扩容机制：容量2的幂，`(n-1) & hash` 代替取模，扩容时元素位置要么在原位置，要么在 `原位置+oldCap`
>   - put 流程：计算hash → 找桶 → 桶为空就插入 → 桶非空就判断key是否相等 → 相等覆盖value → 不相等链表追加/红黑树插入 → 超过阈值扩容
> - `HashTable`：**线程安全**（方法上 `synchronized`），性能差，**已基本被淘汰**，不要用
> - `ConcurrentHashMap`：JDK7 是**分段锁 Segment**，JDK8 之后是**CAS + synchronized + 链表/红黑树**，**线程安全且性能好**
> - `LinkedHashMap`：维护了**双向链表**记录插入顺序，可做LRU
> - `TreeMap`：红黑树，key 有序

---

### 八股 2. ThreadLocal 了解吗？用来干啥？底层数据结构？强弱引用？内存泄漏？
> - **是什么**：线程局部变量，每个线程有自己独立的副本，线程间互不干扰
> - **用途**：① 跨方法传参（如Spring的`RequestContextHolder`传用户信息）；② 线程隔离（数据库连接、Session）；③ 事务上下文传递
> - **底层结构**：`Thread` 类里有一个 `ThreadLocalMap` 字段（实际是 `Entry[]`），`Entry` 继承 `WeakReference<ThreadLocal>`，key 是 ThreadLocal 弱引用，value 是强引用
> - **内存泄漏**：
>   - **key 是弱引用**：ThreadLocal 对象在没有外部强引用时会被GC回收
>   - **value 是强引用**：如果线程一直存活（如线程池核心线程），value 永远不会被回收 → **内存泄漏**
>   - **解决方案**：用完调用 `threadLocal.remove()` 清理 value
> - **项目里用到的场景**：Spring 的 `RequestContextHolder` 持有当前请求用户信息；短链项目里日志traceId也可以用ThreadLocal传

---

### 八股 3. Java 多态：有什么作用？
> - **多态**：同一行为具有不同表现形式
> - **三大前提**：① 继承；② 方法重写；③ 父类引用指向子类对象
> - **作用**：
>   1. **消除耦合**：调用方只依赖抽象（父类/接口），不依赖具体实现
>   2. **扩展性强**：新增子类不影响调用方代码（开闭原则）
>   3. **简化代码**：父类引用统一调用，子类各自实现细节
> - **两种形式**：
>   - **编译时多态（静态）**：方法**重载**（overload），参数列表不同
>   - **运行时多态（动态）**：方法**重写**（override），子类覆盖父类，**实际调用看对象类型而不是引用类型**
> - **底层原理**：方法区的方法表 + 实际类型的虚方法表（vtable），`invokeinterface` / `invokevirtual` 指令动态分派

---

### 八股 4. 怎么保证线程并发安全？
> 分三层：
> 1. **代码层**：
>    - **无共享**：ThreadLocal（线程封闭）
>    - **无状态**：Servlet 设计成无状态（不存成员变量）
>    - **不可变**：`final`、`String`、`Integer`、枚举
> 2. **JDK 并发包（`java.util.concurrent`）**：
>    - **原子类**：`AtomicInteger`（CAS）
>    - **锁**：`ReentrantLock`、`ReadWriteLock`、`StampedLock`
>    - **并发容器**：`ConcurrentHashMap`、`CopyOnWriteArrayList`
>    - **同步器**：`CountDownLatch`、`CyclicBarrier`、`Semaphore`
> 3. **JVM/关键字层**：
>    - `synchronized`（悲观锁，可重入）
>    - `volatile`（可见性 + 禁止指令重排，不保证原子性）
> 4. **分布式层**：
>    - Redis 分布式锁（`SETNX` / Redisson `RLock`）
>    - Zookeeper 分布式锁
>    - 数据库悲观/乐观锁
> - **项目里**：`ShortLinkServiceImpl` 用 Redisson `RLock` 分布式锁防缓存击穿

---

### 八股 5. 锁升级过程
> **Java 对象的锁状态**（MarkWord 层面，synchronized 锁升级）：
> 1. **无锁** → 2. **偏向锁** → 3. **轻量级锁（自旋锁）** → 4. **重量级锁**
>
> | 状态 | 标志位 | 适用场景 | 性能 |
> |---|---|---|---|
> | **无锁** | 01 | 还没人争用 | - |
> | **偏向锁** | 01 | 同一线程反复进入（无竞争） | 几乎无开销 |
> | **轻量级锁** | 00 | 多线程交替执行（短时竞争） | 自旋消耗CPU |
> | **重量级锁** | 10 | 激烈竞争 | 阻塞，线程切换开销大 |
>
> **升级路径**：
> - **无锁 → 偏向锁**：第一个线程进入，CAS 替换 MarkWord 里的 ThreadID
> - **偏向锁 → 轻量级锁**：其他线程来争用，撤销偏向锁，升级为自旋
> - **轻量级锁 → 重量级锁**：自旋超过 `自适应自旋次数`（默认10次）或 `-XX:PreBlockSpin`，升级为 OS 互斥锁
>
> **JDK 15 之后偏向锁被废弃**（因为撤销成本高、收益小），直接走 轻量级 → 重量级

---

### 八股 6. 线程池了解吗？项目里用到没？
> - **七参数**（`ThreadPoolExecutor`）：
>   1. `corePoolSize`：核心线程数
>   2. `maximumPoolSize`：最大线程数
>   3. `keepAliveTime`：空闲线程存活时间
>   4. `unit`：时间单位
>   5. `workQueue`：任务队列（`ArrayBlockingQueue` / `LinkedBlockingQueue` / `SynchronousQueue`）
>   6. `threadFactory`：线程创建工厂（自定义线程名）
>   7. `handler`：拒绝策略（`Abort/CallerRuns/Discard/DiscardOldest`）
>
> - **执行流程**：提交任务 → 核心线程数未满 → 创建核心线程 → 满 → 进队列 → 队列满 → 创建非核心线程 → 到 max → 触发拒绝策略
>
> - **项目证据**：`project/.../RedisStreamConfiguration.java:54-68` 自定义了 Stream 消费者线程池
> ```java
> return new ThreadPoolExecutor(1, 1, 60, TimeUnit.SECONDS,
>     new SynchronousQueue<>(),
>     runnable -> {
>         Thread thread = new Thread(runnable);
>         thread.setName("stream_consumer_short-link-stats_" + index.incrementAndGet());
>         thread.setDaemon(true);
>         return thread;
>     },
>     new ThreadPoolExecutor.DiscardOldestPolicy());
> ```
>
> - **短链项目为啥不用Executors.newFixedThreadPool**：避免 `LinkedBlockingQueue` 默认 `Integer.MAX_VALUE` 容量导致 OOM

---

### 八股 7. Spring 有哪些注解？
> 分模块记：
>
> **核心（IoC/DI）**：
> - `@Component` / `@Service` / `@Repository` / `@Controller`：把类注册成Bean
> - `@Autowired` / `@Resource` / `@Inject`：依赖注入
> - `@Scope`：Bean作用域（singleton/prototype/request/session）
> - `@Value`：注入配置
> - `@Configuration` + `@Bean`：JavaConfig
>
> **Web/MVC**：
> - `@RestController` / `@RequestMapping` / `@GetMapping` / `@PostMapping`
> - `@RequestBody` / `@ResponseBody` / `@PathVariable` / `@RequestParam`
> - `@ControllerAdvice` + `@ExceptionHandler`：全局异常处理
>
> **AOP**：
> - `@Aspect` / `@Pointcut` / `@Before` / `@After` / `@Around` / `@AfterReturning` / `@AfterThrowing`
>
> **事务**：
> - `@Transactional`：声明式事务
>
> **缓存**：
> - `@Cacheable` / `@CachePut` / `@CacheEvict`
>
> **Spring Boot**：
> - `@SpringBootApplication`（= `@Configuration` + `@EnableAutoConfiguration` + `@ComponentScan`）
> - `@ConditionalOnProperty` / `@ConditionalOnClass` / `@ConditionalOnBean`
> - `@ConfigurationProperties`：配置类绑定
>
> **Spring Cloud**：
> - `@EnableDiscoveryClient` / `@EnableFeignClients` / `@EnableCircuitBreaker`
> - `@FeignClient` / `@SentinelResource`

---

### 八股 8. MySQL 调优了解过吗？
> - **慢查询日志**：
>   - 开启：`SET GLOBAL slow_query_log = ON;`
>   - 阈值：`long_query_time`（默认10s，建议调到 1s 或 500ms）
>   - 工具：`mysqldumpslow` 分析日志
> - **EXPLAIN** 看执行计划，重点看：
>   - `type`：访问类型，**`system > const > eq_ref > ref > range > index > ALL`**，至少要 `range` 级别，最好到 `ref`
>   - `key`：实际使用的索引
>   - `rows`：扫描行数（越小越好）
>   - `Extra`：
>     - `Using filesort`：需要优化（建合适索引避免排序）
>     - `Using temporary`：需要优化（避免临时表）
>     - `Using index`：**覆盖索引**，好
>     - `Using where`：回表后过滤
>   - `key_len`：索引使用长度（联合索引用了几个字段）
>
> - **其他调优手段**：
>   - `SHOW PROFILE` / `performance_schema` 查具体耗时
>   - 索引优化（最左前缀、覆盖索引、避免函数）
>   - SQL 重写（避免 `SELECT *`、大表分页用子查询/游标）
>   - 架构层（读写分离、分库分表）

---

### 八股 9. 索引的底层结构（B+树相关）
> - **B+ 树**（InnoDB 默认）：
>   - **多路平衡查找树**，非叶子节点只存**键**（不存数据），叶子节点存**键+数据+指针**
>   - 叶子节点之间用**双向链表**串起来 → 范围查询极快
>   - 树高一般 **3~4 层**（千万级数据），单次查询磁盘IO 2~4次
> - **为什么不用 B 树 / 红黑树 / Hash**：
>   - **B 树**：非叶子节点也存数据 → 单节点能容纳的键更少 → 树更高 → IO 更多
>   - **二叉树/红黑树**：树太高（深度=数据量级），百万级数据要查20+层，IO爆炸
>   - **Hash 索引**：等值查询O(1)很快，但**不支持范围查询**和排序，MySQL的Memory引擎才用
> - **InnoDB 索引分类**：
>   - **聚簇索引**：叶子节点存完整行数据（按主键组织），一张表只有**一个**
>   - **二级索引（非聚簇）**：叶子节点存**主键值**，查非主键字段需要**回表**（先查二级索引拿到主键，再回聚簇索引查完整数据）
> - **覆盖索引**：要查的列都在索引里，**不需要回表**，性能最高

---

### 八股 10. Redis Stream 数据结构了解过吗？
> - **是什么**：Redis 5.0 引入的**持久化消息队列**，可以理解为**加强版的 List + Pub/Sub + 消费组**
> - **核心概念**：
>   - **Stream**：消息流，本质是消息链表，每条消息有唯一 ID（`ms-seq`）
>   - **Consumer Group**：消费组，多个消费者共同消费，互不重复
>   - **XADD**：发布消息
>   - **XREADGROUP**：消费组读消息（带"未确认列表"PEL）
>   - **XACK**：确认消费
>   - **XPENDING / XCLAIM**：处理未确认消息
> - **和 Kafka / RabbitMQ / RocketMQ 的对比**：
>   - 优点：Redis 原生、轻量、简单、与 Redis 生态集成
>   - 缺点：**消息堆积性能下降**（链式结构）、**无高级特性**（无事务消息、延迟消息、广播）
>   - 适用：轻量 MQ 场景，**吞吐量不是特别大**的业务
>
> - **项目证据**：`project/.../RedisStreamConfiguration.java` 项目用 Redis Stream 做短链统计的削峰
>   - `SHORT_LINK_STATS_STREAM_TOPIC_KEY = "short-link:stats-stream"`
>   - `ShortLinkStatsSaveProducer.send()` 用 `opsForStream().add()`
>   - `ShortLinkStatsSaveConsumer.onMessage()` 用消费组消费

---

### 八股 11. Redis 常见数据结构
> | 数据结构 | 用途 | 项目举例 |
> |---|---|---|
> | **String** | 缓存、计数器、分布式锁 | `GOTO_SHORT_LINK_KEY` 短链缓存 |
> | **Hash** | 对象存储 | 用户信息hash |
> | **List** | 队列、最新列表 | - |
> | **Set** | 标签、共同好友、去重 | `SHORT_LINK_STATS_UV_KEY`（UV去重） |
> | **ZSet** | 排行榜、滑动窗口限流 | - |
> | **BitMap** | 布隆过滤器底层、签到 | 布隆过滤器 |
> | **HyperLogLog** | UV 基数统计（省内存） | - |
> | **GEO** | 地理位置 | - |
> | **Stream** | 消息队列 | 短链统计消费 |
> | **Bloom Filter（Module）** | 缓存穿透防护 | 项目核心 |

---

### 八股 12. Redis 集群（高可用、哨兵选举）
> - **三种部署模式**：
>   1. **主从复制**：一主多从，主写从读，**主挂了从不会自动切换**
>   2. **哨兵模式（Sentinel）**：在主从基础上加哨兵进程，**监控+自动故障转移**
>      - 哨兵集群通过 **Raft 算法** 选举 Leader
>      - 主节点客观下线（ODOWN）后，选举新主
>      - 选举规则：① 优先级 `replica-priority`；② 复制进度 `replication offset`；③ runid
>   3. **Cluster 模式**：Redis 官方集群，**数据分片**（16384 槽位），多主多从
>
> - **脑裂问题**：
>   - 哨兵模式下，主从切换瞬间可能出现两个主
>   - 解决：① 至少 3 个哨兵；② 客户端配置多个Redis节点；③ 设置 `min-replicas-to-write 1`
>
> - **短链项目**：用 Redisson 操作 Redis，建议生产用 **Cluster 模式** + **多副本**

---

### 八股 13. 项目中 MQ 怎么使用？为什么不直接创建线程来完成任务？
> **项目里 MQ 使用场景**：
> - **短链访问统计削峰**：用户每次访问短链，`restoreUrl` 都会发一条统计消息到 Redis Stream（`short-link:stats-stream`），后台消费者异步批量落库
> - **延迟队列**：`DelayShortLinkStatsProducer` 用延迟消息做"30分钟后还没访问就归档"
> - **证据**：
>   - 生产：`ShortLinkStatsSaveProducer.send()`（项目里实际用的是 Redis Stream 作为轻量MQ）
>   - 消费：`ShortLinkStatsSaveConsumer.onMessage()`
>
> **为什么不直接创建线程完成任务**：
> 1. **资源不可控**：高并发下短链QPS可能上万，每条访问都开线程 → 线程数爆炸，**OOM**
> 2. **任务不可控**：没有持久化，JVM崩溃后任务丢失
> 3. **解耦**：跳转主流程和统计主流程解耦，跳转 P99 延迟不受统计阻塞
> 4. **削峰填谷**：访问量激增时，MQ 缓冲，消费者按能力消费
> 5. **可观测性**：MQ 自带消息积压、QPS、消费者健康度等监控
> 6. **失败重试**：MQ 自带重试机制，不需自己实现

---

### 八股 14. 哪个项目更有亮点？项目中分表怎么做的？查询时没有携带分片键怎么办？
> **最有亮点**：SaaS 短链接项目（自研的"高并发+海量存储+多租户"系统，参考业界真生产）
>
> **分表做法**（项目：`shardingsphere-config-dev.yaml`）：
> - **t_link** 按 `gid` 哈希分 16 张表
> - **t_link_goto** 按 `full_short_url` 哈希分 16 张表（路由表，存"短链→gid"映射）
> - **t_group** 按 `username` 哈希分 16 张表
>
> **查询时没有分片键怎么办**（**广播表 / 全路由表 / 基因法**）：
> 1. **广播表（字典表）**：如果是不变的字典数据，配置成 `broadcast-table`，每个分片都有一份完整数据
> 2. **ER 绑定（绑定表）**：主子表的关联字段绑定，自动走同分片 JOIN，避免笛卡尔积
> 3. **基因法**：把分片基因（如gid的hash后几位）注入到非分片字段，查询时反推出分片位置（项目里没用，但可参考）
> 4. **全路由表**：维护一张"全量索引表"，用非分片字段查询时先查索引表拿到分片键，再回查
> 5. **妥协方案**：不带分片键时直接**全分片扫描**（ShardingSphere会发广播SQL到所有分片，然后归并结果）
>
> **项目里**：跳转流程天然带 `full_short_url` → 先查 `t_link_goto`（按 full_short_url 分片）→ 拿到 gid → 再查 `t_link`（按 gid 分片）→ 这就是**两步跳转**设计

---

### 八股 15. 项目排查过性能问题没？
> - **使用 Jmeter 压测**（项目里在 `short-link.test.jmx` 有压测脚本）
>   - **短链跳转接口**：QPS 目标 1万+（带 Redis 缓存后单实例轻松到 2万+）
>   - **短链创建接口**：QPS 目标 1000+（受布隆过滤器和分表写入限制）
> - **压测关注指标**：
>   - **QPS / TPS**：每秒请求数
>   - **响应时间**：P50 / P95 / P99
>   - **错误率**：4xx / 5xx 比例
>   - **服务器指标**：CPU / 内存 / 磁盘IO / 网卡流量
>   - **DB 指标**：慢查询数、连接数、主从延迟
> - **典型问题定位**：
>   - CPU 高 → 查热点方法（arthas / async-profiler）
>   - 接口慢 → 链路追踪（SkyWalking）
>   - DB 慢 → 慢查询日志 + EXPLAIN

---

### 八股 16. AI 接触的多吗？
> - **部署过本地模型**：用 Ollama 跑过 Llama 3 / Qwen 2.5 / DeepSeek
> - **日常使用**：Claude / GPT-4 写代码、做Code Review
> - **API 集成**：用过 OpenAI SDK、Anthropic SDK、LangChain
> - **简单应用**：做过 RAG 知识库（向量数据库用 Chroma/Milvus）、智能客服demo

---

### 八股 17. 怎么选合适模型？
> **按需求分层选**：
> 1. **能力 vs 成本**：
>    - **复杂推理 / 创作**：Claude Opus 4.7 / GPT-4o / DeepSeek-R1
>    - **通用场景**：Claude Sonnet 4.6 / GPT-4o-mini / Qwen-Plus
>    - **简单任务 / 分类 / 提取**：Claude Haiku 4.5 / Qwen-Turbo / 国产开源小模型
> 2. **延迟敏感**：选小模型 + 流式输出
> 3. **数据安全 / 私有化**：开源模型本地部署（Qwen2.5、Llama 3.1、DeepSeek）
> 4. **多模态需求**：GPT-4o（图文）、Whisper（语音）、Sora（视频）
> 5. **中文场景**：Qwen / 文心 / GLM / DeepSeek 通常更优
> 6. **长上下文**：Claude Sonnet 4.6（200K）、Gemini 1.5 Pro（2M）
>
> **选型流程**：明确任务 → 选 3~5 个候选 → 准备评测集（输入+期望输出）→ 同prompt跑批 → 按业务指标（准确率/延迟/成本）排序

---

### 八股 18. 听过智能体吗？
> - **Agent（智能体）**：能**自主感知环境、决策、执行**的 AI 系统
> - **核心能力**：
> 1. **任务规划（Planning）**：把复杂任务拆成子任务（ReAct、Plan-and-Execute）
> 2. **工具使用（Tool Use）**：调用外部API/函数（搜索、计算、数据库）
> 3. **记忆（Memory）**：短期上下文 + 长期向量记忆
> 4. **反思（Reflection）**：自我评估结果，迭代优化
> 5. **多智能体协作（Multi-Agent）**：多个Agent分工协作（如 MetaGPT、AutoGen）
>
> - **RAG（Retrieval-Augmented Generation）**：
>   - **是什么**：检索增强生成，先从知识库检索相关内容，再让 LLM 基于检索内容生成答案
>   - **解决**：LLM 知识陈旧、幻觉、垂直领域知识不足
>   - **流程**：用户问题 → 向量化 → 向量数据库检索 Top-K → 拼到 Prompt → LLM 生成
>   - **项目里可应用**：短链项目控制台可以加"AI 助手"，问"如何创建短链"时检索文档回答

---

### 八股 19. 文本太大怎么分块？
> - **固定长度分块**（最简单）：每 N 字（500~1000）切一刀，**缺点**：可能切碎语义
> - **按段落 / 句子切分**：保留语义完整，**常用**
> - **滑动窗口分块**：相邻chunk有 overlap（如 200字重叠），避免边界信息丢失
> - **结构化分块**：按 Markdown 标题 / HTML 标签 / 代码函数切分
> - **语义分块（进阶）**：用嵌入模型算相邻段落相似度，相似度高合并，低则切分
> - **关键考虑**：
> 1. **Chunk Size**：太小检索不准，太大召回噪声多，一般 500~1000 token
> 2. **Overlap**：一般 10%~20%
> 3. **元数据保留**：标题、来源、页码，方便引用
> 4. **小文档处理**：太短就直接整篇作为一个chunk

---

### 八股 20. 全文检索听过吗？
> - **是什么**：对文本内容建立**倒排索引**，实现快速关键词检索
> - **代表工具**：Elasticsearch（最流行）、Solr、OpenSearch、Meilisearch
> - **倒排索引**：维护 "词 → 文档ID列表" 的映射，搜一个词能 O(1) 找到所有相关文档
> - **和 MySQL LIKE 的区别**：
>   - LIKE 是全表扫，慢
>   - ES 倒排索引，亚毫秒级响应
> - **适用场景**：电商商品搜索、日志分析（ELK）、文档检索
> - **短链项目里**：可以加 ES 做"短链描述/标题"的全文检索

---

### 八股 21. 还知道哪些 AI 的？
> - **MCP（Model Context Protocol）**：
>   - **是什么**：Anthropic 提出的**模型上下文协议**，标准化 LLM 与外部工具/数据源的通信
>   - **类比**：像 USB-C 接口，统一了 LLM 接各种工具的方式
>   - **架构**：MCP Host（Claude Desktop等） ↔ MCP Client ↔ MCP Server（提供具体工具）
>   - **价值**：以前每个 LLM 集成工具要单独写适配，MCP 之后工具只需实现 MCP Server，就能被所有支持 MCP 的 LLM 使用
> - **Function Calling / Tool Use**：LLM 按 Schema 调用外部函数（OpenAI、Anthropic都支持）
> - **A2A（Agent to Agent）**：Google 提出的智能体间通信协议
> - **Prompt Engineering**：提示词工程（CoT、Few-shot、ToT）
> - **Fine-tuning**：微调（LoRA、QLoRA、PEFT）
> - **Embeddings**：向量化（OpenAI Embedding、BGE、M3E）
> - **向量数据库**：Milvus / Qdrant / Pinecone / Weaviate / Chroma

---

### 八股 22. LangChain 了解过吗？
> - **是什么**：Python/JS 框架，用于**构建 LLM 应用**，把模型、提示词、工具、记忆、链等模块化
> - **核心组件**：
>   - **Model I/O**：封装各种 LLM（OpenAI、Anthropic、HuggingFace）
>   - **Prompt Template**：提示词模板
>   - **Chains**：组合多个步骤（LLMChain、SequentialChain、RouterChain）
>   - **Agents**：基于 ReAct 框架的智能体
>   - **Memory**：对话记忆（ConversationBufferMemory、SummaryMemory）
>   - **Retrievers**：检索器（对接向量库）
>   - **Tools**：工具集（搜索、计算、API）
> - **进阶**：
>   - **LangGraph**：基于图的工作流编排（适合复杂多步Agent）
>   - **LangSmith**：可观测性平台，调试 + 监控
>   - **LangServe**：把 LangChain 应用部署成 REST API
> - **同类框架**：LlamaIndex（更专注RAG）、Semantic Kernel（C#）、Spring AI（Java）
> - **Java 生态建议**：项目是 Spring Boot 生态，可以关注 **Spring AI**，集成更顺滑

---

## 🎯 总结

| 分类 | 关键能力 | 项目体现 |
|---|---|---|
| **缓存** | Cache Aside + 多级缓存 + 布隆过滤器 | Redis缓存短链、Caffeine本地缓存、`RBloomFilter` |
| **分库分表** | ShardingSphere HASH_MOD 分片 | t_link 按gid、t_link_goto 按full_short_url |
| **一致性** | 先更DB再删缓存、延迟双删、MQ重试 | updateShortLink + delete缓存 |
| **高可用** | 分布式锁、熔断、限流、多租户 | Redisson RLock、流量风控Filter |
| **消息队列** | 异步削峰、最终一致、延迟消息 | Redis Stream 短链统计消费 |
| **并发** | 锁升级、ThreadLocal、线程池 | 自定义Stream消费线程池 |

**面试建议**：重点讲 **"短链跳转"和"分表选型"** 这两个最易被深挖的点，能把"缓存穿透/击穿/雪崩 + 一致性 + 分表 + 布隆过滤器"讲透，基本可以稳住中高级P6/P7面试。
